# Presets

Reference hardware configurations in YAML, following the schema in the top-level README section 8.

Each preset names the published design it approximates and lists what was simplified. Presets are approximations, not exact reproductions.
