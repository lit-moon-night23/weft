# WeftBench

Frozen workloads, fixed input sets, and the runner for each suite version (starting with `weftbench-v1`).

Once a suite version is released, its contents never change. Changes go into a new version.
